const fs = require('fs');

async function run() {
  const apiKey = process.env.API_KEY || "YOUR_API_KEY"; // Wait, I don't have the user's API key.
  // I will just look at the request structure.
}
run();
